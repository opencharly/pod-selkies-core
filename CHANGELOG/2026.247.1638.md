# 2026.247.1638 — chore: align wl-pixelflux pins to v2026.247.1546

## Summary

Aligns this repo's two pixelflux wl-layer pins to v2026.247.1546 (their merged newest tags). The stale pins (.1830/.1823) resolve their contents at pod-selkies v2026.243.1737, one of the dual-pin warning sources across the selkies graph that blocks the charly cachyos-import re-vendor (opencharly umbrella pin-hygiene workstream, recorded in plan/crabbox-loop-cutover.md).

## How tested

```
charly box validate   # repo pod-selkies-core, this head
charly box validate: OK — checked 53 candies, 7 distros, 7 builders; 0 warnings, 0 errors
```

The validate output above is the graph resolution of this head's composition: the resolver fetched the new wl tags and resolved the full graph at 0 warnings, 0 errors — proving the new pins compose cleanly and the stale-tag warning source is gone at this repo. The full downstream graph gate (the cachyos import) re-runs at the distro re-vendor PR once all coordinated pin alignments land; this PR proves its own composition at 0/0.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the pin change. |
| R1 — RCA every anomaly | The dual-pin warning traced to this repo's stale wl pins (their content pins pod-selkies at the old tag); fixed at the source. |
| R2 — finish the whole cutover | Two stale refs aligned to the merged newest tags. |
| R3 — no duplication | One canonical newest version per candy. |
| R4 — no workarounds | Pin to the merged tags; no suppressions. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | box validate 0 warnings / 0 errors pasted (real run). |
| R9 — binary equals source | Fresh binary via the charly repo's bin. |
| R10 — fresh disposable proof | Resolution validated at 0/0 on the committed tree. |

## Change classification

Dependency pin alignment (config-only, 2 lines). **Attribution:** analysed on a live system — the confidence value for this change class (repo composition resolved at 0/0 on a live run of the fresh binary; the full-gate task verify / graph-wide re-vendor is the coordinated downstream PR, not run here for this config-only change). Footer confidence: high confidence.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*