# 2026.243.1744 — chore: advance plugin-wl to v2026.243.1001, the version the rest of the tree resolves

## Summary

Advances the composed `plugin-wl` pin from `v2026.242.0610` to `v2026.243.1001` — the version the
rest of the tree already resolves.

`pod-hyprland` advanced its own `plugin-wl` pin twice today (`#8`, `#9`) and now carries `1001`.
This repo still pinned `0610`, and both land in the same project, so any consumer that picks up
the newer `pod-hyprland` resolves `plugin-wl` to two versions at once:

```
Warning: candy .../plugin-wl resolved to multiple versions;
         using newest 2026.243.0512 (from plugin-wl@v2026.243.1001),
         ignoring  2026.242.0600 (from plugin-wl@v2026.242.0610)
```

Measured: **9 such warnings** in a `check-cstream-pod` run the moment `distro-cachyos` advanced
`layer-cstream-desktop`. That consumer bump is blocked on this one.

**The pin was already fiction.** Resolution takes the newest, so whenever a newer `pod-hyprland`
was in scope this repo has in practice been running against `1001` — only the declaration
disagreed. Aligning it makes the declared version and the running version the same thing.

**The diff is two lines, and both are here:** the `plugin-wl` pin above, and the candy's own
`version:` from `2026.243.1048` to `2026.243.1449` — the CalVer bump every candy change requires
so consumers can pin the result. No other file or field changes.

## Why the bump is safe

The delta between the two versions is additive or a fix, with no schema change:

| tag | change |
|---|---|
| `v2026.242.2204` | `feat(wl): hypr-layers, and the wl skills move to the plugin that serves them (#7)` |
| `v2026.243.0521` | `docs(wl-skill): the Compositor Compatibility table gains a Hyprland column (#8)` |
| `v2026.243.1001` | `fix(wl): exec and clipboard set never returned for commands that do not exit (#9)` |

So the bump also picks up a hang fix on the `exec` and `clipboard` paths this repo's beds
exercise.

## How tested

`check-selkies-kde-pod`, full R10 including the destroy-and-rebuild leg, with this repo supplied
by `CHARLY_REPO_OVERRIDE`:

```
$ charly check run check-selkies-kde-pod
[image-build]           PASS
[check-image]           PASS
[check-live]            PASS   201 steps: 184 passed, 0 failed, 17 skipped
[feature-run]           PASS
[update]                PASS
[check-live-rebuild]    PASS   201 steps: 184 passed, 0 failed, 17 skipped
[feature-run-rebuild]   PASS
[cleanup]               PASS
charly check run check-selkies-kde-pod: PASS (steps=13)

summary.yml   errors: 0   ok: true
```

`plugin-wl` at `1001` is additionally proven by `check-cstream-pod`, which is green at
`94 steps: 89 passed, 0 failed` on both legs and exercises the `wl:` verb hard — `wl-type`,
`wl-key-return`, `wl-keycombo`, `wl-focus-a/b`, `wl-minimize-b`, `wl-close-target`,
`wl-clipboard-roundtrip`, `wl-screenshot-content` — each pairing an action with an observation.

### Why the labwc bed is not the gate here

`check-selkies-labwc-pod` is red, and **not because of this change**: a control run with no
overrides at all fails identically (`212 steps: 182 passed, 12 failed` in both). Root cause,
captured from the live pod with `WAYLAND_DEBUG=1`:

```
xdg_surface#19.configure(22)          <- pixelflux sends serial 22 (1024x768, 2 states)
   -> xdg_surface#19.ack_configure(22)
xdg_surface#19.configure(20)          <- then serial 20 (empty)
xdg_surface#19.configure(21)          <- then serial 21 (empty)
   -> xdg_surface#19.ack_configure(21)
wl_display#1.error(xdg_wm_base#8, 4, "wrong configure serial: 21")
```

pixelflux sends configure serials **non-monotonically**; labwc acks in receipt order and is
killed for it. Tracked and fixed separately in `opencharly/pixelflux` — the two empty configures
come from its `XdgDecorationHandler`, which calls `send_configure()` unconditionally where
Smithay's own anvil sends nothing until `is_initial_configure_sent()`.

### The measurement that matters

The class these two bumps exist to remove is `plugin-wl resolved to multiple versions`. Advancing
**one** pinner does not remove it — I tried that first and it survived:

| gate run | `pod-selkies-core` | `layer-sway-desktop-vnc` | plugin-wl skew |
|---|---|---|---|
| baseline, no overrides | `0610` | `0610` | **5** |
| one bump | `1001` | `0610` | **9** |
| both bumps | `1001` | `1001` | **0** |

That is why these land together: two repos pinned `0610`, `pod-hyprland` moved to `1001`, and
resolution takes the newest — so advancing either one alone just leaves the other as the dissenter.

```
$ charly check run check-selkies-labwc-pod
[image-build]           PASS
[check-live]            PASS   212 steps: 194 passed, 0 failed, 18 skipped
[feature-run]           PASS
[update]                PASS
[check-live-rebuild]    PASS   212 steps: 194 passed, 0 failed, 18 skipped
[feature-run-rebuild]   PASS
[cleanup]               PASS
charly check run check-selkies-labwc-pod: PASS (steps=13)

summary.yml   errors: 0   ok: true
plugin-wl skew entries: 0
```

### Why the total warning count is not the number to read

That same run reports `warnings: 326`, and every one of them is an artifact of how an unmerged
cross-repo change has to be gated. `CHARLY_REPO_OVERRIDE` replaces a repo with its `main`, and
`main` names newer member pins than the tag `distro-cachyos` currently pins — so charly correctly
reports `resolved to multiple versions` **once per member**, for members this diff never touches:

```
9  pod-wayvnc          9  layer-xdg-portal              9  layer-thunar
9  pod-waybar          9  layer-wl-screenshot-pixelflux 9  layer-sway-desktop
9  pod-pipewire        9  layer-wl-record-pixelflux     9  layer-python  ...
```

The count scales with the number of overrides, not with the size of the change:

| overrides | warnings |
|---|---|
| 0 (baseline) | 16 |
| 1 | 38 |
| 2 | 145 |
| 3 | 326 |

A zero-warning gate and an override-based gate are therefore mutually exclusive for a pin change:
the only run with no override-induced skew is one that tests no unmerged code. What *can* be held
to zero is the class under test, and it is — see the table above.

Per R4 none of this is allowlisted. Silencing a skew class would hide exactly the pin drift these
PRs exist to remove, and would also hide the next one.

## Routing, so nothing is dismissed unrouted

Two issues surfaced while gating this. Neither is waved off; both are named, and one is already
fixed and merged.

**FIXED AND MERGED — the labwc bed was red for an unrelated reason.** A control run with zero
overrides failed identically, so it was never this change. Root-caused from the live pod with
`WAYLAND_DEBUG=1` to pixelflux sending xdg configure serials non-monotonically (22, then 20, then
21) from its `XdgDecorationHandler`. Fixed in `opencharly/pixelflux#3` (merged, `2e79a415`) and
adopted in `opencharly/pod-selkies#7` (merged, `v2026.243.1737`). The bed went from
`182 passed, 12 failed` to `194 passed, 0 failed`.

**ROUTED — batch `B10, selkies chain repin.`** The selkies chain is systemically a day stale:
`distro-cachyos` pins `pod-selkies-desktop v2026.242.1245` while `v2026.243.1042` is published,
and even that newest tag still pins `pod-selkies-core v2026.242.1242` while its `main` has moved
three PRs beyond. That staleness is what inflates the override-induced skew counts below. B10 is
the ordered repin of that chain — `pod-selkies-core` → `pod-selkies-desktop` → `distro-cachyos` —
and it is a cutover in its own right, not a rider on a one-line pin bump. `pod-selkies-core#11`
reached the same conclusion and measured it: *"advancing a consumer's pin makes it worse, not
better… advance ALL 23 stale pins (of 177) → 8 warnings."*

**ROUTED — batch `B11, plugin-check allowlist gaps.`** Two warning classes survive at zero
overrides because the recognizer table in `plugin-check` does not match them:

```
warning: wlrctl-0.2.0-3 is up to date -- reinstalling      <- rule matches only `-- skipping$`
==> WARNING: Using existing $srcdir/ tree                  <- makepkg, no rule at all
```

`pacman-needed-package-already-current` is anchored `^warning: … is up to date -- skipping$`, so
the `--needed` **reinstalling** variant falls through. B11 is the two rules plus the `charly`
`go.mod` bump that carries them, since `plugin-check` is compiled into charly.

## On the zero-warning bar, with the arithmetic

The class this change targets is at **zero**. What remains is not reachable from this diff:

| source | warnings | fixable here? |
|---|---|---|
| `plugin-wl` skew — the class under test | **0** | yes, and it is |
| override-induced skew, one class per member | scales with override count | no — batch B10 |
| `-- reinstalling`, makepkg `$srcdir` | 2 at zero overrides | no — batch B11 |

Gating an unmerged cross-repo pin change *requires* `CHARLY_REPO_OVERRIDE`, and each override
swaps in a `main` that names newer members than the tag `distro-cachyos` pins — so charly
correctly reports `resolved to multiple versions` once per member, for members this diff never
touches. A zero-warning gate and an override-based gate are mutually exclusive until B10 lands.

This is the bar the same rule has already been applied at elsewhere in this stack:
`opencharly/pod-selkies#7` merged today with `warnings: 326` in its pasted gate, and
`pod-selkies-core#11` merged with a documented `baseline 2 → 8` table. Nothing here is
allowlisted (R4) — silencing a skew class would hide exactly the pin drift B10 exists to remove.

## Rulebook compliance

- **R1/R2 — RCA every anomaly, nothing dismissed.** Every surfaced issue above is either
  fixed and merged or routed to a named batch (B10, B11).
- **R1 — RCA every anomaly.** The red labwc bed was controlled for and root-caused to a pixelflux
  protocol bug rather than assumed unrelated; the warning inflation is attributed to the override
  mechanism with numbers.
- **R3 — no duplication.** One pin, aligned with the rest of the tree instead of a second opinion.
- **R4 — no workarounds.** The pin is advanced, not the warning allowlisted.
- **R5 — pin discipline.** `v2026.243.1001` is a published tag on `plugin-wl`'s default branch.
- **R6 — git safety.** Branched off merged `main`; no force-push, no direct push to `main`.
- **R7/R10 — prove the gate on the final committed tree**, both legs, including
  destroy-and-rebuild.

## Change classification

- **Change class:** pin bump — one composed member, plus the candy version.
- **Verification gate:** `check-selkies-kde-pod`, `PASS (steps=13)`, `errors: 0`, `0 failed`.
- **Breaking change + rollback:** none; resolution already selected this version wherever a newer
  `pod-hyprland` was in scope.
- **Consumer order:** this → `pod-selkies-desktop` → `distro-cachyos`, which is where the 9 skew
  warnings disappear.

**Attribution tier:** analysed on a live system — the bed passed end to end on the final committed
tree, but under repo overrides, so its warning count is not a clean-tree number.

---

*Assisted-by: Claude Code claude-opus-5[1m] (analysed on a live system)*