version: 2026.240.1943
selkies-core:
    candy:
        version: 2026.243.0840
        description: |
            Single shared streaming spine for every selkies flavor; supervises the desktop's Chrome for both labwc and KDE.
            Compositor-agnostic core of the selkies streaming desktop: the pixelflux
            WebRTC transport (selkies) plus the shared desktop fixings every flavor
            (labwc, KDE Plasma) needs — Chrome + CDP, fonts, the wl-* pixelflux
            screenshot/record/overlay tooling, accessibility introspection, an X
            terminal, the terminal-recording stack, and sshd. There is NO compositor
            here and NO compositor-specific panel/notifier — those live in the
            per-flavor metalayer (selkies-desktop for labwc; selkies-kde-desktop for
            KDE Plasma). This is the single shared spine consumed by BOTH flavors
            across ALL GPU configs, so the streaming transport + fixings are defined
            exactly once (R3 — no duplication, no -host/-pod siblings).

            selkies-core's OWN contribution on top of the composed candies is the
            supervised [program:chrome] launcher: it bakes the chrome program into
            the assembled supervisord config and keeps the browser alive for both
            flavors, where the former per-flavor fire-once autostart only ever
            passed by racing Chrome's brief alive window. The scenarios below assert
            that baked supervisor entry (build scope) and that the supervised
            browser stays up with a responsive CDP endpoint after the
            nested-compositor startup-race settles (deploy scope).
        require:
            # plugin-cdp is the OUT-OF-TREE plugin that serves the `cdp:` check verb
            # (the CDP WebSocket client lives there, not in charly's core check surface).
            # This candy AUTHORS `cdp:` checks, so requiring plugin-cdp puts it in every
            # composing image's scanned candy set; the host's collectReferencedPluginWords
            # then build-connects it before the baked `cdp:` checks dispatch — otherwise
            # they SKIP with `unknown verb "cdp"`. (box/<distro> adopts the matching @github
            # pin via reconcile later — the deliberate cross-repo lag; the R10 bed loads the
            # LOCAL candy via the superproject override.)
            - '@github.com/opencharly/plugin-cdp/candy/plugin-cdp:v2026.242.0527'
            # plugin-wl is the OUT-OF-TREE plugin that serves the `wl:` check verb (the
            # Wayland/sway desktop driver lives there, not in charly's core check surface —
            # EXEC-based, driving the venue's compositor via wlrctl/grim/wtype/swaymsg over the
            # executor reverse channel; the screenshot PNG pulls via GetFile). This candy AUTHORS
            # a `wl:` check (screenshot), so requiring plugin-wl puts it in every composing
            # image's scanned candy set; collectReferencedPluginWords build-connects it before
            # the baked `wl:` check dispatches — otherwise it SKIPs with `unknown verb "wl"`.
            # (Same cross-repo reconcile lag as plugin-cdp.)
            - '@github.com/opencharly/plugin-wl/candy/plugin-wl:v2026.242.0610'
        candy:
            - '@github.com/opencharly/pod-selkies:v2026.243.0720'
            - '@github.com/opencharly/pod-pipewire:v2026.243.0508'
            - '@github.com/opencharly/layer-chrome:v2026.239.1623'
            - '@github.com/opencharly/pod-chrome-cdp:v2026.240.1559'
            - '@github.com/opencharly/layer-desktop-fonts:v2026.239.1629'
            - '@github.com/opencharly/layer-wl-tools:v2026.240.0121'
            - '@github.com/opencharly/layer-wl-screenshot-pixelflux:v2026.243.0823'
            - '@github.com/opencharly/layer-wl-overlay:v2026.239.1634'
            - '@github.com/opencharly/layer-wl-record-pixelflux:v2026.243.0823'
            - '@github.com/opencharly/layer-a11y-tools:v2026.240.0201'
            - '@github.com/opencharly/layer-xterm:v2026.239.1631'
            - '@github.com/opencharly/layer-tmux:v2026.240.0202'
            - '@github.com/opencharly/layer-asciinema:v2026.240.0119'
            - '@github.com/opencharly/layer-fastfetch:v2026.239.1627'
            - '@github.com/opencharly/pod-sshd:v2026.239.1637'
        service:
            # Chrome is launched + SUPERVISED here for BOTH selkies flavors (labwc +
            # KDE), instead of the former fire-once launch from each per-flavor
            # compositor autostart. Root cause it fixes: the selkies flavors nest the
            # compositor inside pixelflux's wayland-1, and a Chrome started during the
            # nested compositor's startup-race self-exits (~39s, clean exit 0 — the
            # window-less browser's sole window goes away on the early color-manager
            # re-init) with nothing to relaunch it; the bed only ever passed by racing
            # that ~39s alive window. supervisord relaunches Chrome post-settle, where
            # it stays up indefinitely. The service's wait_for: blocks on wayland-0 (the
            # nested compositor's client socket, created by labwc / startplasma-wayland)
            # before Chrome execs, so autostart=true is self-synchronizing — no
            # per-flavor handoff needed.
            # restart: always relaunches on the clean exit (autorestart=true); start_secs
            # makes the >5s run reset the retry budget so the one startup-race exit
            # never trips FATAL. SELKIES-ONLY: sway-browser-vnc launches Chrome via
            # chrome-sway (not selkies-core) and is unaffected — it is not
            # pixelflux-nested and does not hit the race.
            - name: chrome
              exec: "%(ENV_HOME)s/.local/bin/chrome-wrapper"
              env:
                WAYLAND_DISPLAY: wayland-0
              restart: always
              start_sec: 5
              start_retry: 3
              priority: 30
              stdout: file:/tmp/chrome.log
              enable: true
              scope: system
              # Chrome is a Wayland client and cannot start before the nested
              # compositor's client socket exists. chrome-wrapper has polled for it
              # itself; this is the same wait, declared instead of hand-rolled (B2), so
              # a timeout fails THE SERVICE and appears in its status rather than
              # looking like Chrome crashed on startup.
              #
              # The path mirrors chrome-wrapper's own defaults exactly — the service
              # sets WAYLAND_DISPLAY but not XDG_RUNTIME_DIR, so the :-/tmp fallback is
              # load-bearing, not decoration.
              #
              # Landed BEFORE chrome-wrapper's loop is removed (pod-chrome-cdp), so no
              # image is ever built with neither guard; the redundant window is
              # harmless because both wait on the same path.
              wait_for:
                paths:
                    - "${XDG_RUNTIME_DIR:-/tmp}/${WAYLAND_DISPLAY:-wayland-0}"
                timeout: 30s
        plan:
            - check: the assembled supervisord config carries selkies-core's own [program:chrome] launcher entry
              file:
                file: /etc/supervisord.conf
                exists: true
                contains:
                    - contains: "[program:chrome]"
            - check: that supervised entry execs the chrome-wrapper launcher (self-synchronizing — the service's declared wait_for precondition blocks on the nested compositor's wayland-0 socket first)
              file:
                file: /etc/supervisord.conf
                contains:
                    - contains: "chrome-wrapper"
            - check: the init system reports selkies-core's supervised chrome program as RUNNING
              context:
                - runtime
              service:
                service: chrome
                running: true
            - check: 25s after deploy Chrome is still alive and its internal CDP endpoint on 9223 answers in-container, proving supervisord relaunched the browser past the nested-compositor self-exit instead of the bed merely racing its brief alive window
              exit_status: 0
              stdout:
                - contains: "ok: chrome supervised"
              context:
                - runtime
              command:
                command: |
                    sleep 25
                    pgrep -f "/opt/google/chrome/chrome" >/dev/null 2>&1 || { echo "FAIL: chrome not running 25s after deploy"; exit 1; }
                    curl -sf --max-time 5 http://127.0.0.1:9223/json/version >/dev/null 2>&1 || { echo "FAIL: chrome CDP not responding"; exit 1; }
                    echo "ok: chrome supervised + alive + CDP responsive"
                in_container: true
            - check: file=/etc/supervisord.conf
              id: selkies-chrome-supervisor-baked
              file:
                file: /etc/supervisord.conf
                contains:
                    - contains: "[program:chrome]"
                    - contains: "chrome-wrapper"
            - check: the supervised Chrome browser launched with --no-first-run (the first-run "welcome / make Chrome default" dialog is suppressed) and --start-maximized
              id: selkies-core-chrome-launch-flags
              exit_status: 0
              eventually: 45s
              retry_interval: 3s
              stdout:
                - contains: "ok: browser"
              context:
                - runtime
              command:
                command: |
                    for pid in $(pgrep -x chrome); do
                      cmd=$(tr '\0' ' ' < /proc/$pid/cmdline 2>/dev/null)
                      case "$cmd" in *--type=*) continue ;; esac
                      case "$cmd" in *--no-first-run*) ;; *) continue ;; esac
                      case "$cmd" in *--start-maximized*) echo "ok: browser pid $pid carries --no-first-run + --start-maximized"; exit 0 ;; esac
                    done
                    echo "FAIL: no Chrome browser process carrying --no-first-run + --start-maximized"; exit 1
                in_container: true
            - check: the supervised Chrome window actually fills the streamed virtual screen (maximize took effect, not merely the flag)
              id: selkies-core-chrome-maximized
              cdp:
                method: eval
                tab: "1"
                expression: "(window.outerWidth >= screen.availWidth - 60 && window.outerHeight >= screen.availHeight - 60) ? 'MAXOK' : ('MAXBAD win ' + window.outerWidth + 'x' + window.outerHeight + ' avail ' + screen.availWidth + 'x' + screen.availHeight)"
              eventually: 60s
              retry_interval: 5s
              stdout:
                - contains: "MAXOK"
              context:
                - runtime
            - check: the streamed desktop frame is real composited content, not an all-black / uniform frame — a deterministic frame-content guard so the bed cannot false-green on a blank stream
              id: selkies-core-frame-not-uniform
              wl:
                method: screenshot
                artifact: /tmp/selkies-core-frame.png
                artifact_min_bytes: 5000
                artifact_not_uniform: true
              eventually: 45s
              retry_interval: 5s
              context:
                - runtime

selkies-core-skill:
    skill:
        name: selkies-core
        family: selkies
        owner: selkies-core
        description: |-
            The compositor-agnostic core of the selkies streaming desktop — the pixelflux transport plus the shared desktop fixings consumed by BOTH flavors (labwc and KDE Plasma). MUST be invoked before editing the selkies-core metalayer or reasoning about what is shared vs flavor-specific in the selkies stack.
        content: |-
            # selkies-core — shared streaming spine for all selkies flavors
            
            `selkies-core` is the single shared metalayer underneath every selkies
            streaming-desktop flavor. It exists so the WebRTC transport + the
            compositor-agnostic desktop fixings are defined EXACTLY ONCE (R3) and the only
            thing a flavor adds is its nested compositor.
            
            ## Composition
            
            ```
            selkies-core =
              selkies            # pixelflux (owns wayland-1) + capture-server + traefik:3000 + ffmpeg
              pipewire           # audio (PulseAudio compat)
              chrome + chrome-cdp # browser + CDP proxy / chrome-devtools-mcp
              desktop-fonts
              wl-tools           # wtype/wlrctl/xdotool/wl-clipboard/wlr-randr
              wl-screenshot-pixelflux + wl-record-pixelflux + wl-overlay  # capture-bridge tooling
              a11y-tools         # AT-SPI2 introspection
              xterm tmux asciinema fastfetch
              sshd
            ```
            
            There is **NO compositor and NO compositor-specific panel/notifier** in
            selkies-core — those live in the per-flavor metalayer:
            
            - `selkies-desktop` (labwc flavor) = `selkies-core` + `labwc` + `waybar-labwc` +
              `swaync` + `pavucontrol`.
            - `selkies-kde-desktop` (KDE flavor) = `selkies-core` + `kde-selkies` (Plasma
              ships its own plasma-pa / panel / notifier, so no waybar/swaync/pavucontrol).
            
            ## Why this exists
            
            Before the flavor split, `selkies-desktop` bundled the transport + the 13
            fixings + labwc together, so a second flavor (KDE) would have duplicated all 13.
            Factoring `selkies-core` out makes the nested compositor the ONLY swappable seam
            between flavors (the `labwc` vs `kde-selkies` candy), across every GPU config
            (the encoder is auto-selected at runtime by pixelflux — see
            `/charly-selkies:selkies-kde-desktop` "Encoder is auto-selected").
            
            ## Chrome supervision
            
            selkies-core owns the **supervised `[program:chrome]` supervisord service** that
            runs the streaming desktop's browser for BOTH flavors — declared in
            `candy/selkies-core/charly.yml`'s `service:` block, so labwc and KWin share one
            launcher (R3). Fields: `restart: always` relaunches Chrome on any exit (including
            the clean self-exit a Chrome started during the nested compositor's startup-race
            produces — the relaunch lands post-settle, where Chrome stays up); `autostart`
            defaults true and is **self-synchronizing** because the service's `wait_for:` blocks
            on the `wayland-0` client socket before Chrome execs (no per-flavor
            `supervisorctl start` handoff);
            `start_secs: 5` + `start_retries: 3` let the one startup-race exit reset the retry
            budget rather than trip `FATAL`; `priority: 30`; `env WAYLAND_DISPLAY=wayland-0`.
            
            Because selkies-core owns Chrome, the per-flavor compositor autostarts
            (`candy/labwc/autostart`, `candy/kde-selkies/kde-selkies-session`) do NOT launch
            it. `sway-browser-vnc` is NOT a selkies flavor (it uses the `chrome-sway` candy,
            not selkies-core, and is not pixelflux-nested) and is unaffected. There is no
            Chrome eventlistener / crash-listener; a wedged crash loop is cleared by
            restarting the container (see `/charly-selkies:chrome` "Chrome supervision").
            
            ## Related
            
            - `/charly-selkies:selkies` — the pixelflux transport at the heart of the core.
            - `/charly-selkies:selkies-desktop-layer` — the labwc flavor metalayer.
            - `/charly-selkies:selkies-kde-desktop` — the KDE Plasma flavor metalayer.
            - `/charly-image:layer` — candy authoring; the unified `service:` schema.
